query mutationDogByName($name: String!) {
  mutationDogByName(name: $name) {
    name
    breed
    ageInWeeks
    image
    sex
    description
    color
    attributes {
      key
      value
    }
  }
}
